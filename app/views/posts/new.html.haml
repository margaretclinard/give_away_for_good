= simple_form_for @post do |f|
  = f.input :category
  / Need to work on dates within form
  / = f.input :date_posted
  / = f.input :deadline
  = f.input :description
  = f.button :submit, "Publish Need"