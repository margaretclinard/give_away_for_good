.index-header
  %p Who Gives?

%hr

.index-header
  %p Charities Involved

- @users.each do |user|
  .hover-tile-outer
    .hover-tile-container
      .hover-tile.hover-tile-visible
      .hover-tile.hover-tile-hidden
        = link_to "#{user.company}", user_posts_path(user)
        %p Description: #{user.bio}